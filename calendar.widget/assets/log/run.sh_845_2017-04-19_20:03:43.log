
function setWorkingDir(){
  if [ ! -e oauth.sh ]; then
    cd assets
  else
    :
  fi
}

setWorkingDir
+ setWorkingDir
+ '[' '!' -e oauth.sh ']'
+ :

function readCredVar(){ #rename as this is confusing this applies to extracting all values in a file after a colon :
  #$1 = file name 
  #$2 = var name e.g. CLIENT_ID
  local credVar=$(sed -e 1b "$1" | grep "$2" | sed 's/.*://' | sed 's/"//' | sed '$s/"/ /g' | xargs)
  echo "$credVar"
}

readonly PARENT_DIR=${PWD%/*}
+ readonly 'PARENT_DIR=/Users/george/Library/Application Support/Übersicht/widgets/calendar.widget'
+ PARENT_DIR='/Users/george/Library/Application Support/Übersicht/widgets/calendar.widget'
readonly COFFEE_FILE_NAME=$(ls ../ | grep .coffee)
ls ../ | grep .coffee
++ ls ../
++ grep .coffee
+ readonly COFFEE_FILE_NAME=calendar.coffee
+ COFFEE_FILE_NAME=calendar.coffee
readonly COFFEE_FILE="$PARENT_DIR"/"$COFFEE_FILE_NAME"
+ readonly 'COFFEE_FILE=/Users/george/Library/Application Support/Übersicht/widgets/calendar.widget/calendar.coffee'
+ COFFEE_FILE='/Users/george/Library/Application Support/Übersicht/widgets/calendar.widget/calendar.coffee'
readonly GOOGLE_APP=$( readCredVar "$COFFEE_FILE" GOOGLE_APP )
 readCredVar "$COFFEE_FILE" GOOGLE_APP 
++ readCredVar '/Users/george/Library/Application Support/Übersicht/widgets/calendar.widget/calendar.coffee' GOOGLE_APP
sed -e 1b "$1" | grep "$2" | sed 's/.*://' | sed 's/"//' | sed '$s/"/ /g' | xargs
+++ sed -e 1b '/Users/george/Library/Application Support/Übersicht/widgets/calendar.widget/calendar.coffee'
+++ grep GOOGLE_APP
+++ sed 's/.*://'
+++ sed 's/"//'
+++ sed '$s/"/ /g'
+++ xargs
++ local credVar=calendar
++ echo calendar
+ readonly GOOGLE_APP=calendar
+ GOOGLE_APP=calendar
readonly APP=$( echo "./$GOOGLE_APP.sh" )
 echo "./$GOOGLE_APP.sh" 
++ echo ./calendar.sh
+ readonly APP=./calendar.sh
+ APP=./calendar.sh

function isNetworkAlive(){
  ./networkAlive.sh
}

function runApp(){
  "$APP"
}

until isNetworkAlive; do
  sleep 10
done
+ isNetworkAlive
+ ./networkAlive.sh
ping: sendto: No route to host
+ sleep 10
+ isNetworkAlive
+ ./networkAlive.sh
ping: sendto: No route to host
+ sleep 10
+ isNetworkAlive
+ ./networkAlive.sh
ping: sendto: No route to host
+ sleep 10
+ isNetworkAlive
+ ./networkAlive.sh
ping: sendto: No route to host
+ sleep 10
+ isNetworkAlive
+ ./networkAlive.sh
ping: sendto: No route to host
+ sleep 10
+ isNetworkAlive
+ ./networkAlive.sh
ping: sendto: No route to host
+ sleep 10
+ isNetworkAlive
+ ./networkAlive.sh
ping: sendto: No route to host
+ sleep 10
+ isNetworkAlive
+ ./networkAlive.sh

runApp
+ runApp
+ ./calendar.sh
